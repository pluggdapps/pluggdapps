[DEFAULT]
debug = True
test = this

[webmounts]
webapp = subdomain, oxy, %(here)s/webapp.ini

[pluggdapps]
test = that
logging.level = debug
logging.format = [%%(levelname)1.1s proc:%%(process)s %%(module)s:%%(lineno)d]

[webapp:webapp]
irequest = somethingElse

[plugin:httprequest]
host = 127.0.0.1
port = 5001
multiprocess = 0

[plugin:httpresponse]
icookie = newcookie
